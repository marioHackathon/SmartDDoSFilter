[TESTS]
  Improve the usage of wrk, making use of weight values for each IP.
  Create http flood and bogus flood for testing purpose.
[WAF]
  Improve the WAF rules set.
  Allow the max size configuration per URI and not globally.
[NFTLB]
  We are currently using a ssh tunnel as a temporal solution, in the close future we will implement HTTPS support for nftlb listener instead.
  Study the use of syscall with getrandom function to solve the srand seed issue.
